using Xunit;

namespace TimeDataViewer.UnitTests
{
    public class UnitTest1
    {
        // MethodUnderTest_Scenario_Behavior()
        [Fact]
        public void Test1()
        {
            // Подготовка(Arrange) объектов, то есть создание и настройка
            // Воздействие(Act) на объект
            // Утверждение(Assert) об ожидаемом результате
        }
    }
}
